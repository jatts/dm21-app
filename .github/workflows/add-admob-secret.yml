name: Add AdMob Test Secret

on:
  workflow_dispatch:

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Setup AdMob Test ID
        run: |
          echo "AdMob Test App ID: ca-app-pub-3940256099942544~3347511713"
          echo "This is Google's official test/sandbox App ID for development"
